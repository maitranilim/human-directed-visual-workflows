# Workflow: Visual brief schema

Copy and complete this before generation.

```yaml
job:
  deliverable:
  audience:
  channel:
  desired_viewer_response:

canvas:
  aspect_ratio:
  pixel_dimensions:
  safe_areas:

source:
  source_class: "none | owned | licensed | private reference"
  shareable: "yes | no | output only"
  important_anchors:

allowed_delta:
  change:
  location:

preservation_locks:
  identity:
  camera:
  geometry:
  semantic:
  exact_text:
  brand_or_cultural:

art_direction:
  primary_style:
  supporting_style:
  composition:
  palette:
  lighting:
  texture:
  typography:

negative_constraints:
  - ""

acceptance:
  must_pass:
  maximum_iterations:
  manual_finish_required:

measurement:
  start_time:
  generation_count:
  accepted_iteration:
  reusable_fragments:
```

Use `not applicable` instead of leaving a critical field ambiguous.
